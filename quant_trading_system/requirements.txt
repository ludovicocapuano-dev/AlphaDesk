# AlphaDesk v2 — Dependencies
# pip install -r requirements.txt

# Core
httpx>=0.25.0
websockets>=12.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Scheduling
APScheduler>=3.10.0

# Technical Analysis & Stats
statsmodels>=0.14.0

# Data Sources
yfinance>=0.2.30
pandas-datareader>=0.10.0

# Database
# (sqlite3 is built-in)

# ── ML Ensemble (v2) ──
torch>=2.1.0
# For CPU-only VPS, install with:
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# Optional: Telegram notifications
# (uses httpx, already included)
